import 'package:flutter/material.dart';


class Detections extends StatelessWidget {
  const Detections({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Detections'),
    );
  }
}